# start an interactive rebase from a commit
bind main R !git rebase -i %(commit)^ # signed: -S
bind diff R !git rebase -i %(commit)^ # signed: -S

bind status C !git commit # signed: -S

set log-options = --show-signature
set diff-options = --show-signature
